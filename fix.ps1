$oldApp = [System.IO.File]::ReadAllText("app_old.js", [System.Text.Encoding]::UTF8); $curApp = [System.IO.File]::ReadAllText("app.js", [System.Text.Encoding]::UTF8); $start = "const translations = {"; $end = "// --- DIL VE TEMA"; $oldIdx1 = $oldApp.IndexOf($start); $oldIdx2 = $oldApp.IndexOf($end, $oldIdx1); $oldBlock = $oldApp.Substring($oldIdx1, $oldIdx2 - $oldIdx1); $curIdx1 = $curApp.IndexOf($start); $curIdx2 = $curApp.IndexOf($end, $curIdx1); $curBlock = $curApp.Substring($curIdx1, $curIdx2 - $curIdx1); $newApp = $curApp.Replace($curBlock, $oldBlock); [System.IO.File]::WriteAllText("app.js", $newApp, [System.Text.Encoding]::UTF8);
